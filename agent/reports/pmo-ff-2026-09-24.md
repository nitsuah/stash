# PMO Fast-Forward (pmo-ff) — 2027 Q1 Planning Prep

> 🧭 [[AGENT-MAIN]] · [[projects/scope|Scope]] · [[prompts/PMO|PMO prompt]]

**Date:** 2026-09-24 · **Branch in every repo:** `pmo-ff` · **Scope:** the 17 Tracked repos in [[projects/scope|scope.md]]

## What was done (every repo)

1. **ROADMAP**: removed every completed item (condensed into FEATURES/CHANGELOG) and carried every open 2025/2026 item into a **2027 Q1** section, tagged with its origin quarter. Sections already scheduled for 2027 Q2+ keep their quarter.
2. **TASKS**: removed completed `[x]` items and retagged stale quarter labels (`[Q3-CEO]`, `Milestone: 2026 Q3`, …).
3. **FEATURES / CHANGELOG / README**: added shipped-but-unlisted capabilities, backfilled missing PRs, fixed stale facts (test counts, tool counts, Python versions, broken links, renamed repos).
4. **Breadcrumbs**: a `> 🧭` nav line under the H1 of every doc (relative `./`/`../` links that resolve on GitHub *and* in `agent/repos/`), plus an idempotent README **Docs Index** block (`<!-- docs-index:start/end -->`) listing every doc, nested folders included.

## PRs (docs-only, not merged — review + merge when ready)

| Repo | PR | Notable beyond the standard reset |
|---|---|---|
| agent-board | https://github.com/nitsuah/agent-board/pull/80 | Merged duplicate Q2 showcase items; React 19 / Vite 8 / Express 5 in CHANGELOG |
| auto-apply-plugin (ats-fill) | https://github.com/nitsuah/ats-fill/pull/98 | CHANGELOG had no `v1.0.1`/`v1.0.2` sections despite tags; added |
| avatar | https://github.com/nitsuah/avatar/pull/29 | Colab link pointed at the defunct buildspace fork |
| bb-mcp | https://github.com/nitsuah/bb-mcp/pull/129 | `MCP_API_KEY` gate (#115) was missing from FEATURES/CHANGELOG; broken badges |
| darkmoon | https://github.com/nitsuah/darkmoon/pull/461 | Stale `docs/MULTIPLAYER_*` paths → `docs/projects/multi/` |
| deployer | https://github.com/Nitsuah-Labs/deployer/pull/166 | Near/Mid/Long-term → quarters; upgradeability design kept verbatim |
| farm-3j | https://github.com/nitsuah/farm-3j/pull/350 | RTS MVP P0 closed as done; `0.1.0` date placeholder fixed |
| fire | https://github.com/nitsuah/fire/pull/122 | 4 docs had a mangled `?#` H1; local staged `weekly-checkin-prompt.md` left out |
| games | https://github.com/nitsuah/games/pull/345 | README said 482 tests / ~95% — now 1017 / 98.58% |
| gcp | https://github.com/nitsuah/gcp/pull/70 | — |
| kryptos | https://github.com/nitsuah/kryptos/pull/222 | Completed phases archived **verbatim** (`docs/archive/2026-completed-roadmap-and-tasks.md`); plain-text `Breadcrumb:` lines replaced with links |
| nitsuah-io | https://github.com/Nitsuah-Labs/nitsuah-io/pull/532 | 0% of 2026 Q2–Q4 shipped; Amoy contracts never redeployed; motor-pool → agent-board |
| osrs | https://github.com/nitsuah/osrs/pull/45 | CHANGELOG still said Python 3.10 |
| overseer (vigil) | https://github.com/nitsuah/vigil/pull/234 | Retired `docs/CHANGELOG.md` after merging its 2 unique entries; removed duplicate HANDOFFs |
| skyview | https://github.com/nitsuah/skyview/pull/150 | Marketplace/scheduling/Calendly cutover added to FEATURES; 10 broken links in OWNER_GUIDE |
| vhs | https://github.com/nitsuah/vhs/pull/62 | #55's unchecked `audit-metadata --apply` review carried into TASKS |
| stash | (this PR) | sync recursion + `-Prune`, orphan finder, REPOS-INDEX, this report |

## Needs a human (surfaced during the pass)

- **nitsuah-io**: Register/Domains labs contracts only ever lived on Mumbai (shut down). Chain config says Amoy, but no contract is deployed there. Needs a funded Amoy wallet (use `deployer`'s `deploy/deploy.ts`).
- **kryptos**: three outreach drafts (FOIA, Elonka Dunin, CIA Public Affairs) are ready in `docs/TASKS.md`; each needs a human send.
- **skyview**: marketplace backend production go-live (Neon migrations + Netlify env vars + one live E2E pass). The site's booking CTAs already depend on it.
- **vhs**: review `scripts/audit-metadata.js` dry-run output against the real collection before any `--apply`.
- **deployer**: re-run `npm audit` in Docker; #153/#158 may have closed part of the deferred item.
- **Git identity**: the global git config on this machine has `user.name=openhands` (with the matching OpenHands email), so every local commit (including these PRs) is authored as openhands. Squash-merges on GitHub show `nitsuah`, which is why `main` looks right.

## Out of scope (seen locally, not touched)

- `9router`, `windirstat-mcp`: forks, per scope.md's "Not tracked" table.
- `gcp`'s local `tire/gcp/login-prompt-and-input-validation-2026-09-24` branch: already pushed and merged as #69, so it's safe to delete.
- `opencut` / `opencut-controller` have vault hub notes under `repos/` but aren't in the Tracked table or cloned under `~/code`.

## Vault / Obsidian — next week

Measured with `agent/scripts/find-orphans.py` (orphan = no links in or out, i.e. an unconnected dot in graph view):

| State | Notes | Orphans | `repos/` orphans |
|---|---|---|---|
| Today (2026-09-24) | 486 | **241** | ~160 |
| Simulated: PRs merged + `sync-repos.ps1 -Prune` + `REPOS-INDEX` | 412 | **95** | **8** |

The simulation copied the vault to scratch and ran the updated sync against the local `pmo-ff` checkouts. The 8 remaining `repos/` orphans are the stale `repos/stash/*` and `repos/.github/*` mirrors, which are no longer synced, plus fire's uncommitted local `weekly-checkin-prompt.md`. The other ~87 are `reports/` (73), `notes/`, and `projects/`.

**Plan:**

1. After the upstream PRs merge: `scripts/sync-repos.ps1 -Prune -DryRun`, review, then `-Prune`.
2. Delete `repos/stash/` and `repos/.github/`. Retitle `repos/agent-board.md` (still `# motor-pool`, with 2026-quarter content).
3. Turn `find-orphans.py` into the obn orphan routine/skill: weekly run, trend in `reports/`, and propose links for `reports/`/`notes/`/`projects/` (a generated `reports/` index would clear most of the 73). The Smart Connections plugin and the Obsidian MCP (`mcp__obsidian__*`) can suggest semantic links for notes the finder flags.
4. Optional: have the routine regenerate `REPOS-INDEX` (retired 2026-09-25 for generated hub links; see [[projects/Vault]]) from `scope.md` so a newly tracked repo is linked automatically.
