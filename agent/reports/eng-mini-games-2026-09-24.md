# eng-mini: games — 2026-09-24 (REPORT MODE / DRY RUN)

> Report only — no moves executed, no changes made to the target repo. Selected as one of the 3 most recently updated in-scope repos (`updated_at` 2026-09-24T17:36:47Z; PR #342 "fix: test Tank Battle logic, restore 75% coverage, enforce it in CI", merged 2026-09-24T17:35:46Z — real coverage/test work from the same-day PMO audit #341, not an automated bot commit). Repo discovery used `search_repositories` (`user:nitsuah`, sort=updated); root-file discovery used `search_code` (`repo:nitsuah/games path:/`), since this session's git proxy scopes direct repo/content access (`get_file_contents`) to `nitsuah/stash` only. `vigil` and `ats-fill` were excluded — both rank above `games` by `updated_at` but neither has an entry under `agent/repos/*.md`, so neither is a tracked repo for this routine.

## Root Audit — games/

| File | Category | MINI Rule | Proposed Action |
|------|----------|-----------|-----------------|
| `.dockerignore` | Build/Deploy | Docker reads from build context root; moving without Dockerfile breaks build | **KEEP** |
| `.env.example` | Config/Env | Must stay root | **KEEP** |
| `.gitignore` | VCS Config | Git reads it from repo root | **KEEP** |
| `Dockerfile` | Build/Deploy | Must stay root | **KEEP** |
| `LICENSE` | Legal | Overseer decision — flag for human review | **DEFER (Overseer)** |
| `README.md` | Documentation | root-only (deploy badges) | **KEEP** |
| `netlify.toml` | Build/Deploy | Netlify reads from repo root | **KEEP** |

Notably absent from root: `package.json`/`package-lock.json` (the repo is structured with `app/package.json` and `tools/package.json` instead of a root manifest — a pre-existing monorepo-style layout, not a MINI-scope finding) and `.gitignore` (present per `agent/repos/games.md`'s repo history, but GitHub code search does not index `.gitignore` file content — confirmed by re-running the identical `path:/` query against `nitsuah/stash`, a repo this session can read directly: it also omits `.gitignore` from the results despite it existing at root. Treated as a search-tool blind spot, not an absence.)

## Duplicate / Destination Checks

`agent/repos/games.md` (last reviewed 2026-09-23) flags stale root-level duplicates: *"the repo-root `ROADMAP.md`/`TASKS.md` are stale duplicates still dated 2026-06-08"* alongside the current `docs/` versions. Re-checked today with targeted `filename:` searches for `ROADMAP.md`, `TASKS.md`, `FEATURES.md`, `METRICS.md`, and `CHANGELOG.md` against `repo:nitsuah/games`: **each now resolves to exactly one hit, all under `docs/`.** No root-level copies were found by any of the five targeted searches, and none appear in the root `path:/` listing above either.

This means the stale root duplicates the games.md summary describes have been removed since the 2026-09-23 write-up — root is clean on this front as of today. `agent/repos/games.md` itself appears to be the stale artifact now (a docs/notes-refresh item, out of MINI's scope — flagging for the daily notes/OBN routine rather than acting on it here).

## Proposed Moves (Dry Run — Not Executed)

**None.** All 7 audited root files (the 6 `path:/` search results plus `.gitignore`, which code search doesn't index) are required-root or an existing Overseer-deferred item; none are candidate-move under MINI.md's folder strategy. No planning-doc duplicates remain at root to resolve.

## Files Left Exempt (with rationale)

| File | Reason |
|------|--------|
| `README.md` | Root-only per MINI.md (deploy badges) |
| `LICENSE` | Overseer decision — requires human review |
| `.env.example` | Standard root-required file |
| `Dockerfile` / `.dockerignore` / `netlify.toml` | Standard root-required build/tooling/deploy files |

## Overseer Decisions Required

| Item | Recommendation | Notes |
|------|----------------|-------|
| `LICENSE` | Consider `.github/LICENSE` | Per MINI.md — requires human validation of root sensitivity |

## Out-of-Scope Observations (not a root-file move, flagging for CLEANUP/notes-refresh)

- `agent/repos/games.md` describes root `ROADMAP.md`/`TASKS.md` duplicates that no longer exist on GitHub as of this run — the local repo summary is out of date on this specific point and could use a refresh pass (separate from this MINI report).

## Errors Encountered

None — the 6-item `path:/` result matches the repo-reported `total_count: 6`, and the notable absences (root manifest, `.gitignore`) were independently explained above rather than left as unexplained gaps.

## Suggested Follow-up

1. No moves to schedule — root is already clean.
2. Bring `LICENSE` to the Overseer for a placement decision, consistent with other repos' open Overseer items.
3. Optional: refresh `agent/repos/games.md`'s stale-duplicate note now that the root `ROADMAP.md`/`TASKS.md` copies are gone (notes hygiene, not MINI scope).

---

*Report generated by eng-mini (report mode / dry run) — no changes made to `nitsuah/games`.*
