# eng-mini: kryptos — 2026-09-01 (REPORT MODE / DRY RUN)

> Report only — no moves executed, no changes made to the target repo. Selected as one of the 2–3 most recently pushed in-scope repos (last commit 2026-09-01T01:58:35-04:00).

## Root Audit — kryptos/

| File | Category | MINI Rule | Proposed Action |
|------|----------|-----------|-----------------|
| `.dockerignore` | Build/Deploy | Docker reads from build context root; moving without Dockerfile breaks build | **KEEP** |
| `.env.example` | Config/Env | Must stay root | **KEEP** |
| `.gitignore` | VCS | Must stay root | **KEEP** |
| `Dockerfile` | Build/Deploy | Must stay root | **KEEP** |
| `LICENSE` | Legal | Overseer decision — flag for human review | **DEFER (Overseer)** |
| `README.md` | Documentation | root-only (deploy badges) | **KEEP** |
| `netlify.toml` | Build/Deploy | Netlify reads from repo root | **KEEP** |
| `pyproject.toml` | Package Manifest | Python tooling (build, packaging, linters) discovers this at root | **KEEP** |

## Proposed Moves (Dry Run — Not Executed)

None. Root is already clean — no `CHANGELOG.md`, `FEATURES.md`, `ROADMAP.md`, `TASKS.md`, or other candidate-move documentation files present at root.

## References That Would Need Updating

Not applicable — no moves proposed.

## Files Left Exempt (with rationale)

| File | Reason |
|------|--------|
| `README.md` | Root-only per MINI.md (deploy badges live here) |
| `LICENSE` | Overseer decision — may move to `.github/` but requires human review |
| `Dockerfile` / `.dockerignore` / `netlify.toml` | Deploy configs read from repo root by their respective platforms |
| `pyproject.toml` | Python packaging/tooling manifest, must stay root |
| `.env.example` / `.gitignore` | Standard root-required files |

## Overseer Decisions Required

| Item | Recommendation | Notes |
|------|----------------|-------|
| `LICENSE` | Consider `.github/LICENSE` | Per MINI.md — requires human validation of root sensitivity |

## Out-of-Scope Observation (not a root-file move, flagging for CLEANUP)

- `.playwright-mcp/page-2026-06-27T03-44-21-641Z.yml` and `.playwright-mcp/page-2026-06-27T03-44-23-851Z.png` are tracked in git despite `.playwright-mcp/` being listed in `.gitignore` (line 50) — these were committed before the ignore rule was added and are now orphaned MCP session artifacts. Recommend the CLEANUP agent `git rm --cached` them.

## Errors Encountered

None.

## Suggested Follow-up

1. No root moves needed this run — kryptos root is already MINI-compliant.
2. Bring `LICENSE` to the Overseer for an explicit placement decision (applies uniformly across scoped repos, consider batching this decision once for all of them).
3. Flag the two orphaned `.playwright-mcp/` tracked artifacts to CLEANUP for untracking.

---

*Report generated by eng-mini (report mode / dry run) — no changes made to `nitsuah/kryptos`.*
