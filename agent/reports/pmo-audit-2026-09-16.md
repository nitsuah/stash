# PMO Audit — 2026-09-16

> 🧭 [[reports/pmo-audit-2026-09-24|2026-09-24]] → <!-- nav -->

## Repos audited

Per `PMO.md`'s pre-flight rule ("if a repo is dirty or not on its default branch, skip it rather than disturbing in-progress work"), git status was checked for all 16 in-scope repos before touching anything. Only 2 of 16 were clean and on their default branch:

| Repo | Result |
|------|--------|
| farm-3j | ✅ Audited — see below |
| kryptos | ✅ Audited — see below |

## Repos skipped this cycle (and why)

14 of 16 repos had an active feature/fix/chore branch checked out (not on default branch), so were skipped to avoid disturbing in-progress work:

| Repo | Local branch (default) | Reason skipped |
|------|------------------------|-----------------|
| agent-board | `ci/upload-coverage-artifact` (master) | Not on default branch; also had untracked dirty files (`dashboard/data/`, `tools/opencut*`) |
| auto-apply-plugin | `v2026/roadmap-and-docs-2026-09` (main) | Not on default branch |
| avatar | `chore/py-version-align-deps-pin-dataset-validation` (main) | Not on default branch |
| bb-mcp | `chore/mcp-inspector-validation` (main) | Not on default branch |
| darkmoon | `fix/pre-push-stale-docker-image` (main) | Not on default branch |
| deployer | `v2026/roadmap-and-docs-2026-09` (main) | Not on default branch |
| fire | `feat/plaid-transaction-sync` (main) | Not on default branch |
| games | `dependabot/npm_and_yarn/eslint-10.10.0` (main) | Not on default branch |
| gcp | `v2026/roadmap-and-docs-2026-09` (main) | Not on default branch; also dirty (`.coverage` modified) |
| nitsuah-io | `chore/migrate-mumbai-to-amoy` (main) | Not on default branch |
| osrs | `fix/python-version-ocr-stuck-state` (main) | Not on default branch |
| overseer | `fix/pagination-and-mobile-a11y` (main) | Not on default branch |
| skyview | `fix/cwe598-client-portal-token` (main) | Not on default branch |
| vhs | `fix/dead-wiring-mobile-export-logs` (main) | Not on default branch |

None of these were touched — no branches created, no files edited, no commits. This is a heavier "in-flight work" cycle than usual: 14 of 16 repos currently have an active branch checked out locally, versus the more typical 2-3 seen in past cycles.

## PRs opened this cycle

### 1. [nitsuah/farm-3j#340](https://github.com/nitsuah/farm-3j/pull/340) — `docs(pmo): refresh METRICS.md with 2026-09-16 audit evidence`
- Docs (README/ROADMAP/TASKS/FEATURES/METRICS) were already current (last touched 2026-09-02) and checked out accurate against the codebase — no contradictions found.
- Re-ran the Docker-first test suite (`scripts/run-in-docker.js test:coverage`) for evidence: 440/440 tests still pass; the known coverage-attribution bug from 2026-09-02 (v8 provider reports 0% despite passing tests) is still present, unresolved, not a new regression.
- Filled in Open Issues (0, via `gh issue list`) and PR count context (1 open dependabot PR, no action needed).
- **CI note:** the initial push failed "Lint and Test" (`prettier --check` — the METRICS.md table edit wasn't Prettier-formatted). Fixed by running `pnpm run format` and pushing a follow-up commit. All checks green as of this report (Lint and Test, Build, docker-smoke, CodeQL, Analyze).

### 2. [nitsuah/kryptos#215](https://github.com/nitsuah/kryptos/pull/215) — `docs(pmo): fix broken Docker Fast Coverage command, refresh METRICS.md`
- `docs/TASKS.md` and `docs/ROADMAP.md` were extremely thorough and internally consistent — this repo already self-flags its own doc staleness inline (see ROADMAP's Phase 3 note), and the standing runbook (`stash/agent/repos/kryptos.md`) had been reviewed just 5 days prior (2026-09-11). No contradictions found in either file.
- **Real bug found:** README's documented "Docker Fast Coverage" command's manual `pip install` list is missing `geographiclib`. Running it as-written breaks test collection for 5 modules (`test_k4_geodesy`, `test_k4_geometry_combined_sweep`, `test_k4_known_plaintext_inversion`, `test_k4_three_layer_composite`, `test_k4_three_layer_composite_geometric`) with `ModuleNotFoundError`. `geographiclib` has been a real dependency (`config/requirements.txt`) since the Phase 6 geodesy work landed in late August, but the README snippet was never updated. Fixed.
- Re-ran the corrected command and refreshed `docs/METRICS.md` with real numbers — big jump since the last measurement (2026-08-22), consistent with the volume of Phase 6/7/8 work documented in TASKS/ROADMAP over the same window:
  - Coverage: 89.27% (was 89.35% — flat despite growth)
  - Tests: 1658 passed / 34 skipped (was 1192/28)
  - Test functions (static): 1535 (was 1271)
  - Source files: 133 (was 112)
  - Test files: 208 (was 184)
  - Fast suite duration: 4:21 (was a stale 48.51s/631-test figure from 2026-05-25)
- CI on this PR was still running checks at report time; no failures observed yet.

## Documentation/reality contradictions found

1. **kryptos README** — "Docker Fast Coverage" command is broken as documented (missing `geographiclib`). Fixed in PR #215.
2. **kryptos docs/METRICS.md** — stale by ~3.5 weeks relative to a large volume of shipped work (Phases 6, 7, 8, and three external-review follow-up passes all landed between 2026-08-22 and 2026-09-03, none of which touched METRICS.md's test/coverage counts until now). Fixed in PR #215.
3. **kryptos docs/ROADMAP.md** — carries "Next Review: 2026-09-15", one day overdue as of this audit. Left untouched (roadmap content itself is accurate) but flagged for the next PMO pass or the repo owner.
4. **farm-3j docs/METRICS.md** — Code Coverage note re-confirmed accurate (still broken, not fabricated as fixed). No contradiction, but worth tracking: this coverage-attribution bug in the Docker bind-mount + v8 setup has now been open across two consecutive audit cycles (2026-09-02, 2026-09-16) without a dedicated fix attempt.

## Risks, assumptions, unknowns

- **14-repo skip rate** is unusually high this cycle. Assuming this reflects genuine active development (branch names all look like real in-progress fix/feat/chore work, not abandoned branches) rather than repos stuck mid-flight. Worth a light sanity check next cycle: if the same branches are still checked out a month from now with no PR activity, that's a different signal (stalled work, not active work) and would justify PMO auditing them despite the branch-skip rule.
- **kryptos coverage-attribution bug (farm-3j)** — two audits in a row have now hit and re-confirmed the same Docker/v8-coverage 0%-report bug without anyone dedicating time to actually fix it. Recommend a follow-up ticket if a real coverage percentage is ever needed for reporting (currently non-blocking since the vitest thresholds config is the enforced source of truth, not this report).
- No fabricated metrics — every number changed in both PRs traces to a command actually run this cycle (quoted in each PR body).

## Follow-up actions

- Owner: repo maintainer — merge #340 and #215 once reviewed.
- No owner assigned: bump kryptos `docs/ROADMAP.md`'s "Next Review" date past 2026-09-15 (cosmetic, non-blocking).
- No owner assigned: investigate the farm-3j Docker/v8 coverage-attribution bug if an accurate coverage percentage becomes a reporting requirement.
- Next PMO cycle: re-check whether the 14 skipped repos are still on non-default branches; if so, and if those branches show no recent commits, treat as stale/stalled rather than active-in-progress.
