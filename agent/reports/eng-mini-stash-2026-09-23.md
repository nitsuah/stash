# eng-mini: stash — 2026-09-23 (REPORT MODE / DRY RUN)

> Report only — no moves executed, no changes made to the target repo. Selected as the #1 most recently updated in-scope repo (last commit 2026-09-19T07:44:58Z, PR #101 "metrics: require 3-5min backoff before auto-merge, add native fallback"). Unlike `fire`/`skyview` below, this repo is the one hosting the eng-mini routine itself, so its root was audited directly from the local checkout (`ls -la`) rather than through the GitHub API workaround.

## Root Audit — stash/

| File/Dir | Category | MINI Rule | Proposed Action |
|------|----------|-----------|-----------------|
| `.claude/` | Tooling | Claude Code project config — read from repo root | **KEEP** |
| `.git/` | VCS internal | N/A | **N/A (not a content file)** |
| `.github/` | Community health / CI | Must stay root | **KEEP** |
| `.gitignore` | VCS | Must stay root | **KEEP** |
| `.obsidian/` | Tooling | Obsidian vault config — read from vault root, same class as `.git/` | **KEEP** |
| `LICENSE` | Legal | Overseer decision — flag for human review | **DEFER (Overseer)** |
| `README.md` | Documentation | root-only | **KEEP** |
| `Daily Notes/`, `Weekly Notes/`, `SAAS/`, `agent/`, `atlassian/`, `backend/`, `cloud/`, `database/`, `docs/`, `git/`, `projects/`, `sso/`, `windows/` | Pre-existing top-level content directories | Already organized per repo's own Folder Strategy; not loose root files | **NO ACTION — out of MINI's root-file-hygiene scope** |

## Proposed Moves (Dry Run — Not Executed)

**None.** Root-level *files* in `stash/` are limited to `.gitignore`, `LICENSE`, and `README.md` — all three are required-root under MINI.md. Everything else at the top level is already a directory, and stash's own repo notes record that a full per-directory README audit and reorg already completed in Q3 2026 (`agent/repos/stash.md`: "all top-level directories... now have a README.md"). There is no root clutter to move.

## Files Left Exempt (with rationale)

| File | Reason |
|------|--------|
| `README.md` | Root-only per MINI.md |
| `.gitignore` | Standard root-required file |
| `.claude/`, `.obsidian/` | Tooling reads these from repo/vault root by convention, same class as `.git/` |
| `LICENSE` | Overseer decision — requires human review (per MINI.md Overseer note) |

## Overseer Decisions Required

| Item | Recommendation | Notes |
|------|----------------|-------|
| `LICENSE` | Consider `.github/LICENSE` | Per MINI.md — requires human validation of root sensitivity |

## Out-of-Scope Observations

- Repo's own tracking (`agent/repos/stash.md`) already flags an open item: "Naming and consistency cleanup — a full repo-wide filename-normalization pass is still open" following the `IAS/`→`cloud/iac/` and `CLOUD/`→`cloud/aws/` reorg. That is a naming-consistency task, not a root-hygiene move, and is better suited to a CLEANUP pass than MINI.
- `agent/repos/stash.md` also notes: "Stash is being deprioritized — overseer TASKS P1 item to mark repo private, block PRs, add sanitization checklist." If that P1 lands, this repo would drop out of future MINI's public-repo discovery entirely (see repo-discovery note below).

## Errors Encountered

None — this repo's root was read directly from the local git checkout, so none of the GitHub API scoping issues below apply to it.

## Suggested Follow-up

1. No moves to schedule — root is already clean.
2. Bring `LICENSE` to the Overseer for a placement decision, consistent with other repos' open Overseer items.

---

*Report generated by eng-mini (report mode / dry run) — no changes made to `nitsuah/stash`.*
