# Tire Kick Report — 2026-06-21

## Scope

Repos included in this run (per `stash/agent/projects/scope.md`):

| Repo | Branch | Status |
|------|--------|--------|
| auto-apply-plugin | main | ✅ tested |
| darkmoon | main | ✅ tested |
| farm-3j | main | ✅ tested |
| fire | main | ✅ tested |
| games | main | ✅ tested |
| kryptos | main | ✅ tested |
| nitsuah-io | main | ✅ tested |
| stash | main | ✅ reviewed (no CI) |
| **agent-board** | mini/agent-board/root-cleanup-2026-06-20 | ⚠️ SKIPPED — active feature branch |
| **overseer** | mini/overseer/root-cleanup-2026-06-20 | ⚠️ SKIPPED — active feature branch |
| **vhs** | fix/ui-polish | ⚠️ SKIPPED — active feature branch |

---

## Test Results by Repo

### auto-apply-plugin
- **Language:** Node.js 20 / Vanilla JS
- **Lint:** ✅ ESLint 0 warnings
- **Tests:** ✅ 71/71 (node:test runner)
- **Issue found:** Docker image naming collision — `config/docker-compose.yml` services defaulted to names like `config-test` and `config-lint`, colliding cross-project with other repos that share the same `config/` directory layout.
- **Fix:** PR [#27](https://github.com/nitsuah/auto-apply-plugin/pull/27) — added explicit `image:` fields to all 4 services.
- **Note:** Running `--build` flag is required to avoid stale cached images. The fix makes image names deterministic.

### darkmoon
- **Language:** TypeScript / React / Three.js
- **Lint:** ✅ ESLint 0 warnings, TypeScript `--noEmit` clean
- **Tests:** ✅ 487/492 (5 intentionally skipped), 69 test files — all pass
- **Issues found:** None

### farm-3j
- **Language:** TypeScript / Next.js 15 / pnpm
- **Type-check:** ✅ `tsc --noEmit` clean
- **Lint:** ✅ ESLint 0 warnings
- **Tests:** ✅ 209/209 tests pass
- **Issues found:** None

### fire
- **Language:** Node.js / Vanilla JS (static web app + Express server)
- **Tests:** ✅ 198/198 (152 unit + 11 e2e + 35 server tests)
- **Lint:** ❌ FAILED — ESLint was linting files inside `.claude/worktrees/bright-bouncing-snowflake/` (the active `metrics-work` worktree), causing cascading prettier and no-undef errors
- **Fix:** PR [#43](https://github.com/nitsuah/fire/pull/43) — added `.claude/` to the ESLint `ignores` list in `config/eslint.config.mjs`
- **CI:** ✅ Merged

### games
- **Language:** TypeScript / Next.js / Three.js / React Three Fiber
- **Type-check:** ✅ TypeScript clean
- **Lint:** ✅ ESLint 0 warnings
- **Tests:** ✅ 481/481 unit tests (Jest, 35 suites)
- **Issues found:** None

### kryptos
- **Language:** Python 3.13 / FastAPI / pytest
- **Tests:** ✅ 999 passed, 34 skipped, 26 deselected (fast suite — `not slow`)
- **Lint:** ❌ FAILED — `ruff check` found 38 errors:
  - 23× E501 (line > 120 chars) in test files
  - 12× B905 (`zip()` without `strict=` param) in src/ and tests/
  - 2× F841 (unused variable) in test files
  - 1× B007 (unused loop variable) in test file
- **Additional issue:** `pyproject.toml` has `fix = true` which caused ruff to auto-apply 56 fixes when run via mounted Docker volume during testing. These changes were included in the fix PR.
- **Additional issue:** Mypy was added to pre-commit hooks in #125 but already had 50 pre-existing failures before our changes. Added type annotations to 3 src/ variables and `# type: ignore[...]` to test mock patterns. CI does not check mypy.
- **Fix:** PR [#126](https://github.com/nitsuah/kryptos/pull/126)
- **CI:** ✅ Merged

### nitsuah-io
- **Language:** TypeScript / Next.js 16 / wagmi
- **Type-check:** ✅ `tsc --noEmit` clean (via `config/tsconfig.json`)
- **Tests:** ✅ 214/214 (Jest, 17 suites)
- **Lint:** ❌ FAILED — `next lint` was removed in Next.js 16. The package.json `lint` script (`next lint .`) errors with `Invalid project directory provided, no such directory: /app/lint`
- **Fix:** PR [#388](https://github.com/Nitsuah-Labs/nitsuah-io/pull/388) — replaced `next lint .` and `next lint . --fix` with direct `eslint . --ext .js,.jsx,.ts,.tsx` calls. The `.eslintrc.json` extending `next/core-web-vitals` is picked up automatically.
- **CI:** ✅ Merged

### stash
- **Type:** Reference/scripts repository — no unified test suite, no CI
- **Review:** 15+ year technical archive covering: Atlassian (Jira, Confluence, Bitbucket), SaaS (Okta, ServiceNow, Slack, GitHub, Datadog), AWS cloud, databases (PostgreSQL, MongoDB), SSO/OAuth2/SAML, PowerShell/VBA/Batch Windows tools, Git utilities, and AI agent prompts.
- **Action:** No docker/deploy actions taken per scope instructions.
- **Issues found:** One pending untracked file: `agent/projects/scope.md` (new, unstaged)

---

## Summary of Issues Found & Fixed

| Repo | Issue | PR | Severity |
|------|-------|-----|----------|
| fire | ESLint linting active worktree `.claude/` directory | [#43](https://github.com/nitsuah/fire/pull/43) | Medium — breaks CI lint gate |
| auto-apply-plugin | Docker image name collision across repos sharing `config/` dir | [#27](https://github.com/nitsuah/auto-apply-plugin/pull/27) | Medium — causes stale image to be used silently |
| kryptos | 38 ruff lint errors (E501/B905/F841/B007) + 56 auto-fixable issues | [#126](https://github.com/nitsuah/kryptos/pull/126) | Low-Medium — lint gate fails |
| nitsuah-io | `next lint` removed in Next.js 16; lint script broken | [#388](https://github.com/Nitsuah-Labs/nitsuah-io/pull/388) | Medium — entire lint step non-functional |

## Clean Repos (No Issues)

- **darkmoon** — all checks pass
- **farm-3j** — all checks pass
- **games** — all checks pass

## Skipped Repos

- **agent-board** — active branch `mini/agent-board/root-cleanup-2026-06-20`
- **overseer** — active branch `mini/overseer/root-cleanup-2026-06-20` (also has secondary worktree at `feat+multi-location-file-detection`)
- **vhs** — active branch `fix/ui-polish` (also has secondary worktree at `worktree-refactor-split-frontend`)

---

## Notes

- All repos were at latest `main` before testing (confirmed with `git pull --ff-only`)
- Docker Desktop 29.5.3 used for all test execution
- E2E/Playwright tests were NOT run (require headless browser + deployed service for most repos)
- kryptos slow Monte Carlo tests were NOT run (tagged `not slow` filter applied)
- nitsuah-io nightly Playwright suite was NOT run
